{
  "name": "spendwise-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Regression tests for firestore.rules, run against the Firestore emulator only - never touches the live Firebase project. See invitations.rules.test.js.",
  "scripts": {
    "pretest": "node -e \"require('fs').copyFileSync('../firestore.rules', 'firestore.rules')\"",
    "test": "firebase emulators:exec --project spendwise-rules-test --only firestore \"node invitations.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0"
  }
}
